# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto
#monitor = eDP-1,2560x1600@60,0x0,1

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = vicinae toggle #wofi --show drun
#$browser = dmenu -b -p "󰜏 " -fn "AdwaitaMono Nerd Font-30" -sb "brown" < /dev/null | xargs -I {} firefox "duckduckgo.com/?q={}" 
$browser = firefox 
$notes = ghostwriter
$write = obsidian
$flashcards = anki


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:



# cloud
# exec-once = rclone mount ProtonDrive: cloud --vfs-cache-mode full --vfs-cache-max-size 1G --poll-interval 1m 

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

#env = HYPRCURSOR_SIZE,20
#exec-once = hyprctl setcursor Adwaita 22


# Set the native Hyprland/Wayland cursor theme and size
env = HYPRCURSOR_THEME,Adwaita
env = HYPRCURSOR_SIZE,24 # Adjust this size for your Wayland cursor

# Set the Xwayland/X11 cursor theme and size
env = XCURSOR_THEME,Adwaita
env = XCURSOR_SIZE,24 # This is often double your HYPRCURSOR_SIZE for HiDPI
###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 0
    gaps_out = 3

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgb(37,37,37)
    col.inactive_border = rgba(0000004d)
 
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
    #no_border_on_floating = true # for waybar floating atop windows
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0
    rounding_power = 0

    # Change transparency of focused and unfocused windows
    active_opacity = 1   #by changing the opacity, you can apply blur to all windows?!
    inactive_opacity = 1
	#windowrulev2 = opaque,class:^(firefox|gimp)$ 
	#windowrulev2 = opaque,class:^(evince|loupe)$
	#windowrulev2 = opaque,class:^figma-linux$
	#windowrulev2 = opaque,class:^(virt-manager|virt-viewer)$ 
	#windowrulev2 = opaque,class:^libreoffice.*$

	
	# blur swaync
	# layerrule = blur, swaync-control-center

    shadow {
        enabled = true
        range = 1
        render_power = 0
		ignore_window=true
		color=rgba(75, 75, 75, 0.2)
		offset = 3 2

    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 5
        passes = 1
	ignore_opacity=true
	new_optimizations=true
	xray=false
	popups=true

       # vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = false,

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = -0.20 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
		disable_while_typing = true
		scroll_factor = 0.3
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {workspace_swipe = false}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, G, exec, $terminal
bind = $mainMod, N, exec, $notes
bind = $mainMod, K, exec, $flashcards
bind = $mainMod, O, exec, $write
bind = $mainMod, Q, killactive,
bind = SUPER_SHIFT, E, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, Space, exec, $menu
bind = $mainMod, F, exec, $browser
# lock the system
bind = , xf86poweroff, exec, pactl set-sink-mute @DEFAULT_SINK@ 1 && swaylock -i /home/heartmachine/Pictures/walls/wallhaven-9omqxw.jpg #& sleep 2 && grim ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png

# exec-once = nm-applet &
#bind = $mainMod, P, pseudo, # dwindle
#bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# horizontal windows
bind = $mainMod, Y, togglesplit

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, swayosd-client --output-volume  5 #wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+ 
bindel = ,XF86AudioLowerVolume, exec, swayosd-client --output-volume  -5 #wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, swayosd-client --output-volume mute-toggle #wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, swayosd-client --brightness +2 #brightnessctl -e4 -n2 set 2%+
bindel = ,XF86MonBrightnessDown, exec, swayosd-client --brightness -1 #brightnessctl -e4 -n2 set 1%-
bind = , XF86KbdBrightnessUp, exec, brightnessctl -d 'spi::kbd_backlight' set 10%+
bind = , XF86KbdBrightnessDown, exec, brightnessctl -d 'spi::kbd_backlight' set 10%-

# special keys
#bind = ,XF86LaunchA, exec, dmenu -b -p "search" -fn "space mono-20" -sb "brown" < /dev/null | xargs -I {} firefox "duckduckgo.com/?q={}" 
bind = ,XF86LaunchA, exec, hyprexpo:expo, toggle 
#bind = ,XF86LaunchB, exec, dunstify -u low "$(cat /home/heartmachine/dev/ref.md)" -t 6000 
bind = ,XF86LaunchB, exec, swaync-client -t 

# Requires playerctl
bindl = , XF86AudioNext, exec, swayosd-client --playerctl next #playerctl next
bindl = , XF86AudioPause, exec, swayosd-client --playerctl play-pause #playerctl play-pause
bindl = , XF86AudioPlay, exec, swayosd-client --playerctl play-pause #playerctl play-pause
bindl = , XF86AudioPrev, exec, swayosd-client --playerctl previous #playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# waybar blur
#layerrule = blur,waybar

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
xwayland {
	force_zero_scaling = true
}

# xwayland scaling
exec-once = xrdb -load ~/.Xresources

# wallpaper section
exec-once=/home/heartmachine/.config/myscripts/bg_pick.sh
exec-once=swaybg -i $(cat /home/heartmachine/.config/myscripts/bg.txt) #-m fill

exec-once=/home/heartmachine/.config/myscripts/sd_pick.sh
exec-once=swaybg -i $(cat /home/heartmachine/.config/myscripts/sd.txt) #-m fill

# polkit
exec-once=systemctl --user start hyprpolkitagent.service

#waybar
#exec-once=waybar
bind = $mainMod, B, exec, killall waybar || waybar

#vicinae
exec-once=vicinae server

#screenshot
bind = $mainMod, Z, exec, grim ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png | dunstify "Screenshot of whole screen taken" -t 1000 # screenshot of the whole screen

#notification
exec-once=hyprnotify

#toggle indicator
exec-once=swayosd-server

# widgets fabric
#exec-once =bash /home/heartmachine/dev/fabric/widgets.sh
exec-once= ~/dev/eww/target/release/./eww daemon
exec-once= ~/dev/eww/target/release/./eww open hud
exec-once= ~/dev/eww/target/release/./eww open work-space



########### plugins

# .config/hypr/hyprland.conf
plugin {
    hyprexpo {
        columns = 3
        gap_size = 5
        bg_col = rgb(111111)
        workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1

        gesture_distance = 300 # how far is the "max" for the gesture
    }
}
